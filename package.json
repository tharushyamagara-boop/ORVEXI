{
  "name": "orvexi-corporate",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node index.js",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy --only hosting"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "firebase": "^12.19.0"
  }
}
